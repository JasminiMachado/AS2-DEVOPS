// __tests__/example.test.js

// Teste de soma
test('soma de 1 + 2 é igual a 3', () => {
  expect(1 + 2).toBe(3);
});

// Teste de subtração
test('subtração de 5 - 3 é igual a 2', () => {
  expect(5 - 3).toBe(2);
});

// Teste de multiplicação
test('multiplicação de 3 * 4 é igual a 12', () => {
  expect(3 * 4).toBe(12);
});

// Teste de divisão
test('divisão de 10 / 2 é igual a 5', () => {
  expect(10 / 2).toBe(5);
});

// Teste de verificação de valor
test('o valor deve ser verdadeiro', () => {
  expect(true).toBeTruthy();
});
